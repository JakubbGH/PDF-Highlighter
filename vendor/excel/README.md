# Excel Macro Template

`vbaProject.bin` is the macro project embedded into `.xlsm` exports so the workbook is structurally macro-enabled.

The current binary is the public XlsxWriter sample VBA project. Replace it with a project containing the final zone-refresh macro when the Excel-side automation is finalized; the browser exporter preserves this file as `xl/vbaProject.bin`.
